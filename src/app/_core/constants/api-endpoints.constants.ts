const AUTH: string = 'auth';
const USERS: string = 'users';
const DASHBOARD: string = 'dashboard';

export const API_ENDPOINTS = {
    AUTH,
    DASHBOARD,
    USERS,
};

export const API_TOKENLESS_ENDPOINTS = [AUTH];
